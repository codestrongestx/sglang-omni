diff --git a/sglang_omni/models/qwen3_asr/request_builders.py b/sglang_omni/models/qwen3_asr/request_builders.py
index a2a372f..5164c77 100644
--- a/sglang_omni/models/qwen3_asr/request_builders.py
+++ b/sglang_omni/models/qwen3_asr/request_builders.py
@@ -31,6 +31,7 @@ from sglang.srt.managers.schedule_batch import (
 )
 from sglang.srt.sampling.sampling_params import SamplingParams

+from sglang_omni.profiler.event_recorder import emit as _emit_event
 from sglang_omni.proto import StagePayload
 from sglang_omni.scheduling.sglang_backend import SGLangARRequestData

@@ -166,11 +167,31 @@ def make_qwen3_asr_scheduler_adapters(
         return tokenizer(prompt, add_special_tokens=False).input_ids

     def request_builder(payload: StagePayload) -> Qwen3ASRRequestData:
+        request_id = payload.request_id
         params = payload.request.params or {}
+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_audio_decode_start",
+        )
         audio = load_audio(_audio_source_from_payload(payload))
         audio_duration_s = float(len(audio) / _SAMPLE_RATE)
         fingerprint = _audio_fingerprint(audio)
+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_audio_decode_end",
+            metadata={
+                "samples": int(len(audio)),
+                "duration_s": audio_duration_s,
+            },
+        )

+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_feature_extract_start",
+        )
         extracted = feature_extractor(
             audio,
             sampling_rate=_SAMPLE_RATE,
@@ -189,17 +210,46 @@ def make_qwen3_asr_scheduler_adapters(
         # mask path must be taken.
         num_mel_frames = int(feature_attention_mask.sum().item())
         num_audio_tokens = int(qwen3_asr_num_audio_tokens(num_mel_frames))
+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_feature_extract_end",
+            metadata={
+                "feature_shape": list(features.shape),
+                "num_mel_frames": num_mel_frames,
+                "num_audio_tokens": num_audio_tokens,
+            },
+        )
         logger.debug(
             f"[qwen3-asr] mel_frames={num_mel_frames} "
             f"num_audio_tokens={num_audio_tokens} feat_shape={tuple(features.shape)}"
         )

+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_prompt_build_start",
+        )
         lang_raw = str(params.get("language") or "en").strip().lower()
         forced_language = {"zh": "Chinese", "cn": "Chinese"}.get(
             lang_raw, "Chinese" if lang_raw.startswith("zh") else "English"
         )
         input_ids = _build_prompt_ids(num_audio_tokens, forced_language)
+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_prompt_build_end",
+            metadata={
+                "forced_language": forced_language,
+                "prompt_tokens": len(input_ids),
+            },
+        )

+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_multimodal_package_start",
+        )
         audio_item = MultimodalDataItem(
             modality=Modality.AUDIO,
             hash=_audio_fingerprint_int(fingerprint),
@@ -234,7 +284,22 @@ def make_qwen3_asr_scheduler_adapters(
         positions = torch.arange(seq_len, dtype=torch.long)
         mm_inputs.mrope_positions = positions.unsqueeze(0).expand(3, -1).clone()
         mm_inputs.mrope_position_delta = torch.tensor([0], dtype=torch.long)
+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_multimodal_package_end",
+            metadata={
+                "prompt_tokens": seq_len,
+                "audio_start": audio_start,
+                "num_audio_tokens": num_audio_tokens,
+            },
+        )

+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_sampling_request_start",
+        )
         temperature = float(params.get("temperature") or 0.0)
         if temperature == 0.0:
             # Qwen3-ASR degenerates under pure-greedy (emits only the language
@@ -263,6 +328,16 @@ def make_qwen3_asr_scheduler_adapters(
         )
         req.multimodal_inputs = mm_inputs
         req._codec_suppress_tokens = None
+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_sampling_request_end",
+            metadata={
+                "max_new_tokens": request_max_new_tokens,
+                "temperature": temperature,
+                "prompt_tokens": len(input_ids),
+            },
+        )

         return Qwen3ASRRequestData(
             input_ids=torch.tensor(input_ids, dtype=torch.long),
@@ -278,6 +353,12 @@ def make_qwen3_asr_scheduler_adapters(

     def result_adapter(data: Qwen3ASRRequestData) -> StagePayload:
         payload = data.stage_payload
+        request_id = payload.request_id
+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_result_adapter_start",
+        )
         output_ids = list(data.output_ids or [])
         # Keep the marker handling at token level. Byte-level BPE decode->encode
         # is not an identity transform for all whitespace/Unicode transcripts.
@@ -295,7 +376,7 @@ def make_qwen3_asr_scheduler_adapters(
         engine_time_s = (
             time.perf_counter() - data.engine_start_s if data.engine_start_s else 0.0
         )
-        return StagePayload(
+        result = StagePayload(
             request_id=payload.request_id,
             request=payload.request,
             data={
@@ -307,6 +388,17 @@ def make_qwen3_asr_scheduler_adapters(
                 "modality": "text",
             },
         )
+        _emit_event(
+            request_id=request_id,
+            stage=None,
+            event_name="qwen3_asr_result_adapter_end",
+            metadata={
+                "output_tokens": len(output_ids),
+                "transcript_tokens": len(transcript_ids),
+                "engine_time_s": engine_time_s,
+            },
+        )
+        return result

     return request_builder, result_adapter
